#### Commands to run on cluster:

sbatch experiment.sh

## To see how the training is going:
squeue --me


### Modify all necessary directories in selora_finetuning.py accordingly


### To copy remote folders to this directory
scp -r /source/folder username@student-cluster.inf.ethz.ch:/remote/folder/directory



